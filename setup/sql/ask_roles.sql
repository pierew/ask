CREATE TABLE IF NOT EXISTS `ask_database`.`ask_roles` (
  `idask_roles` INT(11) NOT NULL AUTO_INCREMENT,
  `name` VARCHAR(45) NOT NULL,
  PRIMARY KEY (`idask_roles`))
ENGINE = InnoDB
DEFAULT CHARACTER SET = utf8;
